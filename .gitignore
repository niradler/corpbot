# Scratch clone of the HKUDS/nanobot fork used during development (not vendored here).
_explore/

# Local agent/session scaffolding (plans, working notes, skills) — not part of the repo.
.claude/

# Python
.venv/
.venv*/
**/.venv/
*.egg-info/
build/
dist/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Background task / tool output
*.output

# Secrets (only *.example.* is committed)
*.secret.yaml
.env

# Helm: do NOT vendor the boxy subchart. charts/ is pulled via `helm dependency build`.
# Chart.lock pins the resolved digest; keep it committed for reproducible builds.
deploy/charts/
deploy/*.tgz
