{
  "_comment": "corpbot nanobot config — mount at ~/.nanobot/config.json. Env vars (${...}) substituted at deploy time. SCAFFOLD: fill in provider + allowFrom. boxy tools are NOT configured as an mcpServers entry here: they come from the installed `corpbot` plugin (pip install corpbot), which is the boxy MCP client and injects X-Sandbox-Id per message. Configure it via env: BOXY_MCP_URL (boxy /mcp endpoint) and BOXY_ROUTER_TOKEN (bearer for boxy-router).",

  "provider": {
    "_comment": "TODO: set your LLM provider/model. Key comes from a secret.",
    "name": "anthropic",
    "apiKey": "${ANTHROPIC_API_KEY}"
  },

  "tools": {
    "exec": { "enable": false },
    "web":  { "enable": false },
    "file": { "enable": false }
  },

  "channels": {
    "slack": {
      "enabled": true,
      "token": "${SLACK_BOT_TOKEN}",
      "allowFrom": ["<COMPANY_SLACK_USER_IDS>"]
    }
  }
}
